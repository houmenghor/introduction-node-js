/*
definition
The indexOf method in JavaScript is used to find the index of the first 
occurrence of a specified element in an array. If the element is not found, it returns -1.

Syntax
array.indexOf(element, start)

*/

const array = [1, 2, 3, 4, 5];
const index = array.indexOf(3);
console.log(index); // Output: 2
